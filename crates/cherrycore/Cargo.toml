[package]
name = "cherrycore"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.219", features = ["derive", "rc", "serde_derive"] }
# diesel = "2.2.10"
# diesel-async = { version = "0.3.1", features = ["postgres"] }
